/**
 * @type CardProps - is a type that defines the props of the FormResult components
 */
type CardProps = {
    img: string,
    title: string,
    text: string[],
    link: string,
    left : boolean
}

export default CardProps
